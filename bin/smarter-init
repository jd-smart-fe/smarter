#!/usr/bin/env node

/**
 * 用 init 生成 smarter.config.js 文件
 *
  {
     upload: {
       local:
       remote:
       host:
       port:
       username:
       password:
     }
  }
 */

const program = require('commander');
const chalk = require('chalk');
