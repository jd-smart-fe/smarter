#!/usr/bin/env node

const program = require('commander');

program
  .version(require('../package.json').version)
  .command('init <template>', '创建一个项目脚手架')
  .parse(process.argv);

process.on('exit', () => {
  console.log();
});
